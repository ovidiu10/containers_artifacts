apiVersion: v1
kind: Service
metadata:
  name: tripinsights-userprofile-deployment
  labels:
    run: tripinsights-userprofile-deployment
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: tripinsights-userprofile-deployment